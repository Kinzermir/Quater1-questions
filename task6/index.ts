let personName= ("\t \n John Doe \n \t");
console.log(personName);   //whitespace characters, including "\t" (tab) and "\n" (newline).

// Then print the name after striping the white spaces. means use trim function.
console.log(personName.trim());

// This function tirm creates a new string, strippedName, which is a copy of personName but with the leading and trailing whitespace characters removed.
